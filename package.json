{
  "name": "guitar-scaler-server",
  "version": "1.0.0",
  "description": "An application that helps guitar players learn their scales quickly",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production && npm run start:prod || npm run start:dev",
    "start:prod": "node server.js",
    "start:dev": "concurrently \"nodemon --ignore 'client/*'\" \"npm run client\"",
    "client": "cd client && npm run start",
    "install": "cd client && npm install",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run build",
    "lint": "npx eslint **/*.js --quiet; exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.7"
  },
  "dependencies": {
    "all": "0.0.0",
    "express": "^4.17.1"
  }
}
